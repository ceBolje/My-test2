import React from 'react';

const Title = ({ title }) => (

    <div className="row content-line">
        <div className="col-sm">
            <h1 className="mt4 mb2 text-muted">{ title }</h1>
        </div>
    </div>
);

export default Title;

